# TODO：UI 稳定基座（已完成）

这个 TODO 对应的 UI 基座/复用组件/输入 scope 收敛已经完成并落地。

后续如果 UI 交互回归：
- 先跑 `bun run test -- src/ui/SetupWizard.test.tsx src/ui/permissions/PermissionsDialog.test.tsx src/ui/AgentsDialog.test.tsx`
- 再按 `plans/ui/REGRESSION.md` 做手动回归
