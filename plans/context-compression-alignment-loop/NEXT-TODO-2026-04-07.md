# Context Compression Next TODO (2026-05-11)

目标：把已经完成的 `CCA-120 ~ 123` 这一波 diagnostics / microcompact / reactive / collapse-client mainline，切到下一阶段更接近 Claude Code 剩余差距的 working-set / session-memory / compact-protocol 主线。

## 上一阶段已完成

这一波已经完成，不再作为当前执行主线：

1. `CCA-080` app-server `thread/read` 暴露 `latestRequestCollapse`
2. `CCA-081` 抽官方 `latest collapse summary` reader/helper
3. `CCA-082` `/context` 优先复用 runtime/session collapse facts
4. `CCA-083` app-server `thread/messages` 可选 collapse summary surface
5. `CCA-084` collapse event / diagnostics parity across Web adapters
6. `CCA-085` collapse-aware replay tooling / inspection helpers
7. `CCA-090` 评估最小 persisted collapse state/store

## 当前阶段判断

目前这一波已经完成：

1. `CCA-120` richer assembled-payload diagnostics ledger
2. `CCA-121` microcompact strategy v2
3. `CCA-122` reactive compact shaping v2
4. `CCA-123` richer collapse client consumption / parity

所以当前更适合做的，不再是继续围绕最小 collapse consumption 扩面，而是回到仍然和 Claude Code 有明显差距的 3 条主线：

1. working-set / keep strategy 继续升级
2. session memory 进入更深的 restore consumption
3. compact protocol 的 cross-surface / remote ecosystem 继续补齐

## 推荐顺序

1. post-`CCA-132` mainline re-rank

## Todo

### `CCA-130` working-set selector v3

已完成：
- auto compact 的 working-set anchor 现在会识别最近成功的 `Read` / `Grep` / `Glob` filesystem cluster
- `/context` 会显式暴露 `anchorKind` / `anchorToolNames` / `anchorBacktrackTurns`

### `CCA-131` session memory deeper restore consumption v3

已完成：
- file-backed restore 当前已经有 canonical restore-artifacts helper
- SDK `query(..., { resume })` / `query(..., { continue: true })` 现在也会注入 one-turn session-memory reminder block
- CLI `resumeLast` 与 SDK file-backed restore 已复用同一条 sidecar 读取 / restore-artifacts 组装路径

### `CCA-132` compact protocol ecosystem v2

已完成：
- `latestCompactBoundary` 现在已经真正进入 Web runtime thread cache / display selector / header surface
- 这轮没有再扩 server 协议，而是把现有 compact boundary facts 变成真实 client consumption
- compact protocol 的下一阶段主线需要基于 post-132 状态重新排优先级

## 现在不建议直接做的事

1. 完整 persisted collapse store
2. archived collapse spans / collapse commits
3. replay-time collapse projection rebuild
4. 把 compact protocol 直接做成另一个独立持久化权威模型

## 一句话结论

下一阶段最值钱的方向不是继续给 collapse 叠更多元数据，而是：

> 回到 working-set / session-memory / compact protocol 这三条剩余大差距，把已有能力继续贯通成系统。
