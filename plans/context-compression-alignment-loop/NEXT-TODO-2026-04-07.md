# Context Compression Next TODO (2026-04-07)

目标：把上一阶段已经完成的 collapse 主线，切到下一阶段更高价值的消费层与剩余差距主线，避免继续围绕“更重的 collapse store”投入过早复杂度。

## 上一阶段已完成

这一波已经完成，不再作为当前执行主线：

1. `CCA-080` app-server `thread/read` 暴露 `latestRequestCollapse`
2. `CCA-081` 抽官方 `latest collapse summary` reader/helper
3. `CCA-082` `/context` 优先复用 runtime/session collapse facts
4. `CCA-083` app-server `thread/messages` 可选 collapse summary surface
5. `CCA-084` collapse event / diagnostics parity across Web adapters
6. `CCA-085` collapse-aware replay tooling / inspection helpers
7. `CCA-090` 评估最小 persisted collapse state/store

## 当前阶段判断

目前 request-time collapse 已经具备：

1. request-time MVP
2. diagnostics / contributor / metadata 可见性
3. runtime-visible `collapseState`
4. session persistence `request_collapse_applied`
5. app-server reader / `thread/read` / `thread/messages` / `/context`
6. thread-level inspection helper

所以当前更适合做的，不再是继续堆更重的 persisted collapse store，而是：

1. 让已存在的 collapse state 被真实客户端 surface 消费
2. 回到仍和 Claude Code 差距明显的 working-set / session-memory / restore 主线

## 推荐顺序

1. `CCA-120` richer assembled-payload diagnostics ledger
2. `CCA-121` microcompact strategy v2
3. `CCA-122` reactive compact shaping v2
4. `CCA-123` richer collapse client consumption / parity

## Todo

### `CCA-120` richer assembled-payload diagnostics ledger

目标：
- 让 `/context` 更贴近最终 assembled API payload
- 减少“分阶段估算”和“真实请求体”之间的心理落差

建议实现：
- 为 next-turn diagnostics 引入更明确的 assembled ledger 结构
- 清楚区分：system / request history / current user / injected blocks / tool-exposure 等固定成本

验收标准：
- `/context` 能回答“最终发给模型的 payload 大头是谁”
- text / JSON / app-server payload 语义保持一致

### `CCA-121` microcompact strategy v2

目标：
- 继续补强中间层减压能力
- 扩大 `microcompact` 在高压力下的稳定收益

建议实现：
- 在当前 allow/deny 基础上，增加更细的 tool family / size / recency 策略
- 保持 diagnostics 与 runtime 同步

验收标准：
- 收益更稳定
- 不明显增加误压缩风险

### `CCA-122` reactive compact shaping v2

目标：
- 让 reactive compact 从“单次 fallback”继续走向更成熟的恢复策略

建议实现：
- 继续细化 overflow 分类
- 让 diagnostics 更清楚反映 reactive compact 触发与 fallback 结果

验收标准：
- reactive path 更可解释
- 不引入多次重试或语义漂移

### `CCA-123` richer collapse client consumption / parity

目标：
- 在已有 header summary 之外，继续让 collapse state 进入更可解释的 client surface

建议实现：
- 先从低风险 surface 入手，例如 thread details / diagnostics pane / thread inspection UI
- 优先消费已有 `latestRequestCollapse` / inspection helper，而不是新造 store

验收标准：
- 至少一个额外 client surface 真正消费 collapse state
- 不改变现有 timeline item 协议

## 现在不建议直接做的事

1. 完整 persisted collapse store
2. archived collapse spans / collapse commits
3. replay-time collapse projection rebuild
4. 为了 collapse 重写 session persistence 权威模型

## 一句话结论

下一阶段最值钱的方向不是“让 collapse 更复杂”，而是：

> 先让已存在的 collapse state 真正被 surface 消费，再回到更高价值的 working-set / session-memory / restore 差距。
