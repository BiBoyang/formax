# Context Compression Next TODO (2026-05-12)

目标：在 `CCA-143 ~ 146` 收口之后，不再继续惯性堆叠 reducer，而是切回更高价值的系统差距：working-set / session-memory / canonical-owner / compact protocol。

## 当前阶段判断

目前已经完成：

1. `CCA-140` middle-layer strategy stack scaffolding
2. `CCA-141` tool-result budget replacement v1
3. `CCA-142` cache-aware microcompact v3
4. `CCA-143` snip boundary + MVP v1
5. `CCA-144` middle-layer stage contract / terminal prune fallback v1
6. `CCA-145` strategy coordination facts v1
7. `CCA-146` middle-layer control-plane diagnostics v1

这意味着：

> query-time middle-layer stack 已经成型。  
> 下一阶段不再是“继续找一个新 reducer 往里塞”，而是：
> 1. 提升 task-minimal working-set 选择质量
> 2. 提升 restore 后的 session-memory 实用性
> 3. 继续收敛 stack 周边 flow 的 canonical owner
> 4. 逐步补 compact protocol 的 remote / restore ecosystem

## 推荐顺序

1. `CCA-150` working-set / keep strategy v4
2. `CCA-151` session-memory restore consumption v4
3. `CCA-152` middle-layer canonical-owner convergence
4. `CCA-153` compact protocol remote / restore alignment

## Todo

### `CCA-150` working-set / keep strategy v4

已完成：
- working-set anchor 现在不再只有固定的 1-turn rewind 语义。
- `Read` anchor 继续保持 1 个 extra user turn 的回卷窗口。
- `filesystem_cluster` anchor 现在有独立的 2-turn backtrack window。
- `/context` diagnostics 现在会显式暴露：
  - `anchorKind`
  - `anchorToolNames`
  - `anchorBacktrackTurns`
  - `anchorMaxBacktrackTurns`

当前定位：
- 这是 working-set v4 的第一刀，不是最终 task-minimal selector。
- 它解决的是“filesystem exploration 明明还在当前任务里，但只因多了一轮用户迭代就被视为 stale”的问题。

### `CCA-151` session-memory restore consumption v4

目标：
- 让 session-memory 不只停留在 file-backed restore 的 one-turn reminder 注入
- 继续扩大 restore 后可直接消费的 session-memory surface

验收：
1. 至少一条当前未消费 session-memory 的 restore / resume surface 开始复用 canonical artifacts
2. 不重新引入第二套 sidecar 读取或 reminder 组装逻辑
3. contract / diagnostics 能解释新增 restore consumption 的语义

### `CCA-152` middle-layer canonical-owner convergence

目标：
- 继续减少 `contextCompressionService.ts` 中 stack 外的直接 helper 路径
- 让 middle-layer stack 更接近 surrounding flow 的唯一 owner

验收：
1. 至少一段当前 stack 外的 `microcompact` / `prune` 路径被收敛到 canonical owner
2. persisted vs request-only envelope 边界更清晰，而不是更混
3. 不新增 reducer，只做 owner convergence

### `CCA-153` compact protocol remote / restore alignment

目标：
- 继续补 compact boundary / preserved segment / remote restore 的生态差距

验收：
1. 至少一条 remote / restore surface 开始消费更完整的 compact protocol fact
2. 不引入新的 persisted authority model
3. app-server / Web parity 仍基于 canonical protocol，而不是局部特判

## 现在不建议直接做的事

1. 完整 persisted collapse store
2. archived collapse spans / collapse commits
3. replay-time collapse projection rebuild
4. 再开新的 query-time reducer 主线

## 一句话结论

14x wave 已经把独立中间层策略栈的第一阶段做实。  
接下来最值钱的方向是：

> 提高 working-set / restore / owner-convergence 的系统质量，  
> 而不是继续无边界地扩张 reducer 数量。
