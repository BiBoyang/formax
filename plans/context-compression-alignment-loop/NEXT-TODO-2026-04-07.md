# Context Compression Next TODO (2026-04-07)

目标：把上一阶段已经完成的 collapse 主线，切到下一阶段更高价值的消费层与剩余差距主线，避免继续围绕“更重的 collapse store”投入过早复杂度。

## 上一阶段已完成

这一波已经完成，不再作为当前执行主线：

1. `CCA-080` app-server `thread/read` 暴露 `latestRequestCollapse`
2. `CCA-081` 抽官方 `latest collapse summary` reader/helper
3. `CCA-082` `/context` 优先复用 runtime/session collapse facts
4. `CCA-083` app-server `thread/messages` 可选 collapse summary surface
5. `CCA-084` collapse event / diagnostics parity across Web adapters
6. `CCA-085` collapse-aware replay tooling / inspection helpers
7. `CCA-090` 评估最小 persisted collapse state/store

## 当前阶段判断

目前 request-time collapse 已经具备：

1. request-time MVP
2. diagnostics / contributor / metadata 可见性
3. runtime-visible `collapseState`
4. session persistence `request_collapse_applied`
5. app-server reader / `thread/read` / `thread/messages` / `/context`
6. thread-level inspection helper

所以当前更适合做的，不再是继续堆更重的 persisted collapse store，而是：

1. 让已存在的 collapse state 被真实客户端 surface 消费
2. 回到仍和 Claude Code 差距明显的 working-set / session-memory / restore 主线

## 推荐顺序

1. `CCA-100` collapse summary 真正进入 Web / client surface
2. `CCA-110` working-set / keep strategy v2
3. `CCA-111` session memory deeper restore consumption
4. `CCA-112` remote / cross-surface compact restore parity

## Todo

### `CCA-100` collapse summary 真正进入 Web / client surface

目标：
- 让 `latestRequestCollapse` 不只停留在 parser / contract 层
- 至少一个真实 Web / desktop surface 开始展示并消费它

建议实现：
- 先从低风险位置开始，例如 thread details / diagnostics 面板 / thread header
- 展示最小字段：
  - `phase`
  - `collapsedHeadMessageCount`
  - `estimatedTokensSaved`
  - `recapFingerprint?`

验收标准：
- 不改现有 timeline item 协议
- 没有 collapse 时 surface 可自然隐藏
- thread/read 与 thread/messages 任一来源都可被客户端消费

### `CCA-110` working-set / keep strategy v2

目标：
- 继续缩小和 Claude Code 在“最小工作集选择”上的差距

建议实现：
- 在现有 recent-`Read` anchor 之外，引入更稳的 working-set 判断
- 继续保持 auto compact 与 diagnostics 一致

优先考虑：
- recent files
- plan / todo state
- 当前 mode
- boundary 后 continuation 的最小工作集

验收标准：
- keep 策略更聪明，但不引入明显 tail 膨胀
- `/context` 能解释新的 keep / working-set 行为

### `CCA-111` session memory deeper restore consumption

目标：
- 让 session memory 不只在 restore 后 refresh sidecar
- 而是进入更直接的恢复消费链

建议实现：
- 优先找一个低风险 restore 消费点
- 避免把 session memory 直接硬塞成 persisted history 替代

验收标准：
- restore 后能更直接受益于已有 session memory
- 不破坏 replay / boundary / persisted history 权威语义

### `CCA-112` remote / cross-surface compact restore parity

目标：
- 继续补 compact / boundary / collapse 在跨 surface 下的 restore 一致性

建议实现：
- 从 app-server / thread surface 继续补齐 compact-aware restore 观察面
- 避免 live prompt view 和 replay / thread surfaces 再次漂移

验收标准：
- compact / collapse / restore 的 key facts 在不同 surface 间一致

## 现在不建议直接做的事

1. 完整 persisted collapse store
2. archived collapse spans / collapse commits
3. replay-time collapse projection rebuild
4. 为了 collapse 重写 session persistence 权威模型

## 一句话结论

下一阶段最值钱的方向不是“让 collapse 更复杂”，而是：

> 先让已存在的 collapse state 真正被 surface 消费，再回到更高价值的 working-set / session-memory / restore 差距。
