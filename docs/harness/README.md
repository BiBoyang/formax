# Harness Governance（入口）

本文件是兼容入口，主索引已迁移至 `docs/harness/index.md`。

请优先从以下文档开始阅读：
- `docs/harness/index.md`

说明：
- 该重构将 `harness` 文档按主题拆分到子目录（`contracts/`、`runbooks/`、`inventories/`）。
- 保留 `README.md` 仅用于向后兼容旧引用，不再作为唯一索引维护。
