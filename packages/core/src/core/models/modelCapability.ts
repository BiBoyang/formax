export type { ModelContextWindowMetadata, RuntimeModelProfile } from '../../config/modelCapability.js'
export {
  normalizeIdentityBaseUrl,
  normalizeModelIdentity,
  sameModelIdentity,
  createModelContextWindowMetadata,
  shouldPersistContextWindowSource,
  buildOpaqueFingerprint,
  buildRuntimeModelProfileFingerprint,
} from '../../config/modelCapability.js'
